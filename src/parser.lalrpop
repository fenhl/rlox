use crate::ast::*;

grammar;

pub(crate) Program = Declaration*;

Declaration = {
    Statement,
    //TODO others
};

Statement: Stmt = {
    <Expression> ";" => Stmt::Expr(<>),
    "print" <Expression> ";" => Stmt::Print(<>),
    //TODO others
};

Expression: Expr = {
    "nil" => Expr::Nil,
    //TODO others
};
